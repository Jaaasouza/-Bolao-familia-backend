{
  "name": "usam-fifa-world-cup-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the USAM FIFA World Cup 2026 pool — Express + Postgres",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/db/migrate.js",
    "dev": "node src/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/tests/sim/"
    ]
  }
}
